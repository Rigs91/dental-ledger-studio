# Screenshot Shot List

Use these notes if you want to recapture the README assets manually.

## General capture settings

- Window width: about 1440 px
- Keep the top navigation visible when it helps anchor the page
- Use the default light theme at 100% zoom
- Use the seeded demo data only

## 01-dashboard.png

- Start on `Dashboard`
- Keep visible:
  - `Key metrics`
  - `Action center`
  - `Risk drivers`
  - `Balances to watch`

## 02-patient-detail.png

- Open `Maria Chen 41` from the dashboard
- Keep visible:
  - balance snapshot
  - activity timeline
  - open review flags with at least one visible flag row

## 03-claim-review.png

- From `Maria Chen 41`, open the denied claim timeline
- Keep visible:
  - claim context
  - submission history
  - top of the ledger timeline

## 04-analytics.png

- Open `Analytics`
- Keep visible:
  - priority queue
  - root causes or risk signals
  - action-oriented cards instead of mostly KPI cards with `N/A`
